import {createTheme} from "@mui/material/styles";

const theme = createTheme({
    palette: {
        primary: {
            main: '#46484A',
        },
        secondary: {
            main: '#e16030',
        },
        third: {
            main: '#f5f5f6',
        },

    },
});

export default theme;